class AppConfig {
  // Usar localhost quando estiver em modo USB
  static const String apiUrl = 'http://localhost:3000';
  
  static const int connectionTimeout = 30;
  static const int receiveTimeout = 30;
}
